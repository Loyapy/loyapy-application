import 'package:flutter/material.dart';
import 'package:loyapy/core/core.dart';

class AppColors {
  static const Color defaultPrimaryColor = Colors.blue;
  static final primaryColor =
      kHexToColor('#feee00', primaryColor: defaultPrimaryColor);
  static final scaffoldBGColor =
      kHexToColor('#F8F9FA', primaryColor: defaultPrimaryColor);
  static final secondaryColor =
      kHexToColor('#28A745', primaryColor: defaultPrimaryColor);
  static final darkBGColor =
      kHexToColor('#1A1A2E', primaryColor: defaultPrimaryColor);
  static final borderColor =
      kHexToColor('#E0E0E0', primaryColor: defaultPrimaryColor);
  static final hintColor =
      kHexToColor('#888888', primaryColor: defaultPrimaryColor);
  static final textColor =
      kHexToColor('#333333', primaryColor: defaultPrimaryColor);
  static final iconColor =
      kHexToColor('#555555', primaryColor: defaultPrimaryColor);
  static final successColor =
      kHexToColor('#28A745', primaryColor: defaultPrimaryColor);
  static final errorColor =
      kHexToColor('#DC3545', primaryColor: defaultPrimaryColor);
  static final warningColor =
      kHexToColor('#FFC107', primaryColor: defaultPrimaryColor);

  static final defaultBlackColor = Colors.black;
  static final defaultWhiteColor = Colors.white;
}
